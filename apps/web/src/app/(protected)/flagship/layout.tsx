/**
 * FlagShip Layout
 *
 * Layout for FlagShip feature flag management pages.
 */

export default function FlagShipLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

